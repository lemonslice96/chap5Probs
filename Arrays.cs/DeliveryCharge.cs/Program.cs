using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DeliveryCharge.cs
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] Zip = new int[10];
            float[] Charge = new float[10]
            for(int i = 0; i < Zip.Length; i++)
            {
                Zip[i] = Convert.ToInt32(Console.ReadLine());
                if (Zip[i] > 10000)
                {
                    
                }
            }
        }
    }
}
